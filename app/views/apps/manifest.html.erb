CACHE MANIFEST
# Version: <%= @app.updated_at %> 

CACHE:
<%= @app.bundle_id %>.app
<%= @app.bundle_id %>/icons/icon.png
<% @app.files.each do |file| %>
<%= "#{@app.bundle_id}/#{file}" %>
<% end %>
/app_assets/apps.js
